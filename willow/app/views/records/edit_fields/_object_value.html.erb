<% if current_user.admin? %>
  <%= f.input :object_value, 
  collection: ::Cdm::Selections::ObjectValue.call,  # providing collection will make it a select box by default
  selected: :normal,
  include_blank: false,
  input_html: { class: 'form-control' } # bootstrap class for select box
  %>
<% end %>